apiVersion: v1
kind: Service

metadata:
  name: healthcare-service
  namespace: healthcare

spec:
  selector:
    app: healthcare-app

  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
      nodePort: 30082

  type: NodePort
